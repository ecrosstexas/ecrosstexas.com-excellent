---
layout: base
---

<article class="region page">
  <div class="wrapper flow prose">
    <h1 class=" ">{{ title }}</h1>
    {{ content | safe }}
  <div class="library">
     <ul>
        {%- for book in books -%}
 
        {% if book.Library == 1 %}
        <li><a href="/book/{{ book.ISBN }}/">{{ book.Title }}</a>{% if book.Subtitle %}: {{ book.Subtitle }}{% endif %}</li>
        {% endif %}

        {%- endfor -%}
    </ul>
</div>
</div>
</article>