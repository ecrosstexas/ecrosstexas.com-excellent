---
layout: ecrosstexas-base
masonry: true
---

<div class="lefs-wrapper">
  <header class="full | section" style="--spot-color: var(--color-primary)">
    <article class="intro">
    <div class="[ intro__header ] [ radius frame ]">
      <h1 class="[ intro__heading ] [ weight-normal text-400 md:text-600 ]">
        {{ intro.eyebrow }}
        <em class="">{{ intro.main }}</em>
      </h1>
    </div>
    <div class="[ intro__content ] [ flow ]">
      <p class="intro__summary">{{ intro.summary }}</p>
      <a href="{{ intro.buttonUrl }}" class="button">{{ intro.buttonText }}</a>
    </div>
    <div class="[ intro__media ] [ radius dot-shadow ]">

      <img
        class="[ intro__image ] [ radius ]"
        src="{{ intro.image }}"
        alt="{{ intro.imageAlt }}"
      />
    </div>
  </article>
  </header>

{% set ctaContent = primaryCTA %}
{% include "partials/lefs-cta.njk" %}

{% include "partials/lefs-featured-work.njk" %}

{% include "partials/lefs-studio-feed.njk" %}

{% set ctaContent = cta %}
{% include "partials/lefs-cta.njk" %}


  <section class="full | region">
    <div class="wrapper flow prose">
      <h2>{{ blog.title }}</h2>
      <!-- blog intro text is optional. -->

      {% if blog.intro %}
      <p>{{ blog.intro }}</p>
      {% endif %}

      <ul
        class="feature | list region"
        role="list"
        data-layout="100"
      >
        <!-- loop posts -->
        {% set itemList = collections.posts %} {% for item in itemList.slice(0, 6) %}
        <!-- activate tags -->
        {% set activateTags = true %}
        <!-- set heading context -->
        {% set headingContext = "h3" %}
        <!-- card -->
        {% include 'components/card.njk' %} {% endfor %}
      </ul>
    </div>
  </section>

</div>
